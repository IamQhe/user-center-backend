<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qhe.usercenter.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.qhe.usercenter.model.User">
            <id property="userId" column="user_id" />
            <result property="userName" column="user_name" />
            <result property="userAccount" column="user_account" />
            <result property="userPassword" column="user_password" />
            <result property="userRole" column="user_role" />
            <result property="userStatus" column="user_status" />
            <result property="userAvatarUrl" column="user_avatar_url" />
            <result property="userGender" column="user_gender" />
            <result property="userEmail" column="user_email" />
            <result property="userPhone" column="user_phone" />
            <result property="invitationCode" column="invitation_code" />
            <result property="isDelete" column="is_delete" />
    </resultMap>

    <sql id="Base_Column_List">
        user_id, user_name, user_account, user_role, user_status, user_gender, user_avatar_url,
        user_email, user_phone, create_time, update_time
    </sql>

    <select id="searchUserList" resultType="com.qhe.usercenter.model.UserVO">
        select <include refid="Base_Column_List"/>
        from user
        <where>
            is_delete = 0
            <if test="userId != null">AND user_id = #{userId}</if>
            <if test="userName != null and userName != ''">AND user_name like concat('%', #{userName}, '%')</if>
            <if test="userAccount != null and userAccount != ''">AND user_account like concat('%', #{userAccount}, '%')</if>
            <if test="userRole != null">AND user_role = #{userRole}</if>
            <if test="userStatus != null">AND user_status = #{userStatus}</if>
            <if test="userGender != null">AND user_gender = #{userGender}</if>
            <if test="userEmail != null and userEmail != ''">AND user_email like concat('%', #{userEmail}, '%')</if>
            <if test="userPhone != null and userPhone != ''">AND user_phone like concat('%', #{userPhone}, '%')</if>
            <if test="startCreateTime != null and endCreateTime != null">AND create_time between #{startCreateTime} and #{endCreateTime}</if>
            <if test="startUpdateTime != null and endUpdateTime != null">AND update_time between #{startUpdateTime} and #{endUpdateTime}</if>
        </where>
    </select>
</mapper>
